name: Build
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Install Http Server
        run: npm install --global http-server
      - name: Install dependencies
        run: npm install
      - name: Test and coverage
        uses: ArtiomTr/jest-coverage-report-action@v2
#      - name: Show Path
#        run: cd ~ ; ls -a; ls -d $PWD
#      - name: SonarCloud Scan
#        uses: SonarSource/sonarcloud-github-action@master
#        with:
#          projectBaseDir: /home/runner/work/hello_jest/hello_jest
#          args: >
#            -Dsonar.organization=rohitbansa
#            -Dsonar.projectKey=rohitbansa_hello_jest
#            -Dsonar.javascript.lcov.reportPaths=tests/coverage/lcov.info
#            -Dsonar.test.exclusions=tests/**
#            -Dsonar.tests=tests/
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          
